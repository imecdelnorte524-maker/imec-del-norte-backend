// src/work-orders/enums/ac-inspection-phase.enum.ts
export enum AcInspectionPhase {
  BEFORE = 'BEFORE',
  AFTER = 'AFTER',
}

export enum WorkOrderEvidencePhase {
  BEFORE = 'BEFORE',
  DURING = 'DURING',
  AFTER = 'AFTER',
}